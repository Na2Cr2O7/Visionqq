[permissions]
allow = ["Edit", "Bash=cd QQPilot4; dotnet build -v q --nologo", "Bash=cd QQPilot4; dotnet build QQPilot4.csproj -v q --nologo", "Bash=if (Test-Path \"QQPilot4\\t\") { Get-ChildItem \"QQPilot4\\t\" -Recurse | Select-Object FullName } else { \"t 目录不存在\" }", "Bash=if (Test-Path \"t\\t.csproj\") { \"t\\t.csproj 存在\" } else { \"t\\t.csproj 不存在\" }; if (Test-Path \"t\") { \"t 目录存在\" } else { \"t 目录不存在\" }"]
