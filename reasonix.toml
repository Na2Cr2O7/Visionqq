[permissions]
allow = ["Edit", "Bash=cd QQPilot4; dotnet build -v q --nologo", "Bash=cd QQPilot4; dotnet build QQPilot4.csproj -v q --nologo", "Bash=if (Test-Path \"QQPilot4\\t\") { Get-ChildItem \"QQPilot4\\t\" -Recurse | Select-Object FullName } else { \"t 目录不存在\" }", "Bash=if (Test-Path \"t\\t.csproj\") { \"t\\t.csproj 存在\" } else { \"t\\t.csproj 不存在\" }; if (Test-Path \"t\") { \"t 目录存在\" } else { \"t 目录不存在\" }", "Bash=Get-Command msbuild -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source; if (-not $?) { \"msbuild not in PATH\" }; Get-ChildItem \"C:\\Program Files\\Microsoft Visual Studio\\2022\" -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Name", "Bash=$vswhere = \"C:\\Program Files (x86)\\Microsoft Visual Studio\\Installer\\vswhere.exe\"; if (Test-Path $vswhere) { & $vswhere -latest -products * -requires Microsoft.Component.MSBuild -property installationPath } else { \"vswhere not found\" }", "Bash=$msb = \"C:\\Program Files\\Microsoft Visual Studio\\18\\Community\\MSBuild\\Current\\Bin\\MSBuild.exe\"; if (Test-Path $msb) { & $msb \"VisionQQ_C\\MFCUpdate\\MFCUpdate.vcxproj\" /p:Configuration=Release /p:Platform=x64 /m /v:m /nologo } else { \"MSBuild.exe not found\" }"]
